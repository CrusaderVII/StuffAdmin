spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: C:\MyProjects\StuffAdmin_config
          default-label: main

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka